# Copy to .env.local and fill in values

# Backend URL (FastAPI)
BACKEND_URL=http://localhost:8000

# Next.js public env vars (exposed to browser)
# These are set at runtime via the UI, not here — see localStorage in lib/storage.ts
